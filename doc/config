# The application/database name:
app_name = main
#app_name = side-issue
# database type - file or redis (default to file)
database_type = redis
#database_type = file
# invalid:
#database_type = void
specpath = /home/jtc/.stodo/specs
datapath = /home/jtc/.stodo/data
# Default path for user files, such as attachments:
userpath = /home/jtc/.stodo/user
## Override the path of the "stodo git repository" -
# (default is <datapath>/git) - uncomment/modify to change:
#gitpath = /home/mylogin/mystodo/git
# Example override of standard (/usr/bin) git executable location:
#gitexe = /usr/local/bin/git
# Default email address to use if not specified elsewhere:
default_email = jim.cochrane@gmail.com
# comma-separated list of one or more backup paths:
#backuppath = /nfs/neptune/data/backups/stodo/maindb
backuppath = /home2/backups/jtc/stodo
emailtemplate = /home/jtc/bin/mutt -s <subject> <addrs>
#emailtemplate = /bin/mutt -s <subject> <addrs>
# dummy email client:
#emailtemplate = /home/jtc/bin/mutt -s <subject> <addrs>
# To use a user name other than your UNIX login name, edit/uncomment this:
#user = mymyselfandi
#logtype = syslog
logtype = redis
# The path of the gcalcli script (obsolete):
#calendarcmd = /home/mylogin/lib/python2/bin/gcalcli
# prefix to use for categories in some reports:
categoryprefix = cat-
# To specify no category prefix, uncomment this line:
# categoryprefix =
# Paths to executables for various types of media viewers and editors
# (modify as needed):
view_msword = /bin/libreoffice
edit_msword = /bin/libreoffice
view_msexcel = /bin/libreoffice
edit_msexcel = /bin/libreoffice
view_openspread = /bin/libreoffice
edit_openspread = /bin/libreoffice
view_openxml = /bin/libreoffice
edit_openxml = /bin/libreoffice
# (view here means "run", of course:)
view_executable = 'bash -c'
# (empty string [''] here means to run the executable directly)
#view_executable = ''
edit_executable = /bin/hexedit
view_plain_text = /bin/gview
edit_plain_text = /bin/gvim
view_pdf = /bin/evince
edit_pdf = /bin/libreoffice
view_video = /bin/vlc
#view_video = /bin/mpv
edit_video = /bin/kdenlive
view_code = /bin/gview
edit_code = /bin/gvim
# (view here means "play/listen", of course:)
view_audio = mpv --no-video
edit_audio = /bin/audacity
view_directory = /bin/gview
edit_directory = /bin/gvim
